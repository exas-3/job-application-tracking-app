rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // This app currently uses Firebase Admin SDK for server-side access.
    // Client-side direct Firestore writes are denied by default.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
